/**
 * EverMemOS Integration — Barrel Export
 *
 * Memory Genesis 2026: Wires Liminal Space inner-life data to EverMemOS
 * for episodic trace formation, reconstructive recollection, and
 * semantic consolidation.
 */

export { storeMemory, searchMemories, healthCheck, withEverMemOSFallback } from './client'
export { formatCoherenceMemCell, formatMythEventMemCell, formatCouncilMemCell } from './formatters'
export type { EverMemOSMessage, MemoryResult } from './types'
